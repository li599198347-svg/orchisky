# Prompt 起手模板 — Orchisky 平台版

> 配套文档:《原型设计规范 - Orchisky 平台版 V1.0.md》
>
> 用途:基于 Orchisky 平台的客户 Demo 起手 Prompt,仅覆盖 **PC 后台管理系统** 场景。
>
> 填空约定:`<xxx>` 需替换,`【可选】` 段落按需删除。

---

## 0. 三条通用前置(所有 Prompt 必加)

```
基于 Orchisky 平台(Ant Design 5 定制)开发此原型,严格遵循以下视觉约束:

1. 基础组件与样式基于 Ant Design 5,所有色值 / 圆角 / 间距 / 字重延续 Ant 5 默认 token。
2. 主色 #1677FF,圆角默认 6px(Tag 4px · Modal 8px),控件默认高 32px。
3. 顶栏 48px 深色 #001529 白字,激活 tab 白底蓝字;侧栏白底,激活项 #E6F4FF 药丸 + 蓝字 + 500 字重。
4. 状态标签一律用实色填充小药丸(已审核=蓝填 · 已同步=绿填 · 已驳回=红填 · 冻结=灰填);
   分类类 Tag(如"采购单 / 成品 / 紧急")才用淡底描边。
5. 字体:中文 Noto Sans SC / PingFang SC,数字编码 JetBrains Mono。
6. 表头背景 #FAFAFA,主键列 #1677FF 等宽字链接,行高 44px,hover 背景 #F5F5F5。
7. 表单多列网格(3-4 列),标签右对齐 + 冒号,必填红 * 在标签前,段标题 600 · 15px · 无图标无装饰。
8. 禁止:卡片阴影、彩色表头、侧栏彩色、渐变按钮、emoji、3D 图标、圆角 12px+、标签上浮。
9. 假数据参考真实 ERP 业务(物料编码如 M-BRG-A2024-02 / 供应商如"苏州精密机械有限公司")。
10. 只输出单页 HTML,内联样式用 CSS 变量实现 Ant token,不引入外部 React / Ant 库。
```

---

## 1. LIST · 列表页模板

```
【三条通用前置】

请生成一个 Orchisky 平台的【<模块名>】列表页,结构如下:

【整体布局】
- 顶栏 48px,深色 #001529,左置 Orchisky Logo,一级 Tab,右侧图标区。
- 侧栏 220px,白底,当前模块菜单树。
- 主内容区背景 #FAFAFA,顶部面包屑。

【筛选卡片】(白卡 · 6px 圆角 · 1px #F0F0F0 边)
一行放 3 个三段式筛选条件,字段 → 操作符 → 值。
底部按钮:[提交](主蓝) [重置](默认) [+ 添加条件](链接按钮)

【表格卡片】
表格工具栏:
- 左侧:[筛选] 按钮 + "已选 N 项"
- 右侧:[刷新] [列设置] | [<主批操作>](主蓝) [<反向批操作>](红描边) [删除](红描边) [导出](主蓝)

表格列:☑ | 行号 | 编码(主键蓝色链接) | <业务字段> | 状态(实填Tag) | ERP同步(实填Tag) | 数量(等宽右对齐) | 时间 | 操作

至少 10 行假数据,状态分布要真实:
- 70% "已审核蓝填 + 已同步绿填"
- 20% "待审核橙淡底 + 待同步橙淡底"
- 10% "已驳回红填 + 未同步灰填"

底部分页:右对齐,"共 142 条" + 页码 + "10 条/页" 下拉。
```

### 参数清单

| 占位符 | 说明 | 举例 |
|---|---|---|
| `<模块名>` | 页面功能名 | 采购入库单 / 物料列表 |
| `<业务字段>` | 表格业务列 | 物料编码 / 规格型号 |
| `<主批操作>` | 正向批量操作 | 审核 / 提交 / 同步 |
| `<反向批操作>` | 反向批量操作 | 反审核 / 撤销 |

---

## 2. DETAIL · 详情页模板

```
【三条通用前置】

请生成一个 Orchisky 平台的【<模块名>详情】页,结构如下:

【整体布局】
- 顶栏 / 侧栏同列表页
- 顶部页头:"← <模块名>详情",旁边 Tab [详情](激活)

【分段信息块】(白卡内,段落标题分隔)

段落 1:基础信息
- 4 列网格,每个字段:<标签>: <值>
- 字段清单(8-12 个):<字段列表>
- 部分值可以是 Tag

段落 2:单位与包装
- 同 4 列网格

段落 3:库存控制

【关联子表】(新白卡)
- 卡片标题:"入库明细" / "出库记录"
- 右上角 [导出]
- 表格 6-8 列,5-8 行数据

【底部操作栏】(固定,右对齐)
[编辑](主蓝) [打印] [导出] [返回](Text)
```

---

## 3. CREATE · 新增/编辑页模板

```
【三条通用前置】

请生成一个 Orchisky 平台的【<模块名>新增】页,结构如下:

【整体布局】
- 顶栏 / 侧栏同列表页
- 顶部 "← <模块名>" + Tab [添加](激活)

【分段表单】(白卡内)

段落 1:基础信息
- 4 列表单网格,横向间距 24px,纵向 16px
- 每个表单项:<标签>(右对齐 · 110px · 必填前加红 *): <输入控件>
- 控件:输入框 / 下拉选择 / 日期选择器 / 开关
- 字段清单(8-14 个)

段落 3:明细(子表)
- 标题右侧:[+ 选择物料] [+ 从模板导入]
- 表格 8-12 列(可编辑),初始空态居中灰字

【底部操作栏】(左对齐,1px 顶部分隔)
[提交](主蓝) [保存草稿](默认) [取消](默认)
```

---

## 4. 组合示例

### 4.1 列表页 + 详情抽屉(Drawer)

```
【LIST 列表页模板 完整版】

扩展:点击"查看"链接时,从右侧滑入 720px 宽抽屉 Drawer,
内容为简化版详情页,仅保留段落 1+2 和子表。
底部:[编辑] [关闭]。
```

### 4.2 列表页 + 新增 Modal

```
【LIST 列表页模板 完整版】

扩展:工具栏右侧 [+ 新增](主蓝),点击弹出 Modal:
- 宽 720px
- 标题:"新增<模块名>"
- 正文:简化版新增页,仅基础信息段落
- 底部:[取消] [确定](主蓝)
```

### 4.3 主表 + 子表单据场景

```
【CREATE 新增页模板 完整版】

强调子表结构:
- 主表字段 4-6 个(供应商/客户/经办人/日期)
- 子表可编辑表格,每行末尾有"删除行"图标
- 表头右上角:[+ 从来源单据选择明细] [+ 直接添加物料]
- 子表底部统计:"共 N 行 · 总数量 NNN · 总金额 ¥NNN,NNN"
```

---

## 5. 微调关键词速查

| 症状 | 纠正指令 |
|---|---|
| 主色偏紫/偏深 | "主色精确使用 #1677FF,不要自创" |
| 侧栏变成深色渐变 | "侧栏必须白底,激活项用 #E6F4FF + #1677FF 字" |
| 按钮太圆(胶囊状) | "按钮圆角严格 6px,控件高度 32px" |
| 标签变成圆点+淡底 | "状态标签使用 Ant Tag 实色填充样式" |
| 表单标签在输入框上方 | "表单标签必须右对齐在输入框左侧,加冒号" |
| 段标题加了左竖线 | "段标题仅加粗 600 15px,不加任何装饰" |
| 卡片浮起来带阴影 | "白卡用 1px #F0F0F0 描边,绝不加阴影" |
| 数字列左对齐 | "数字列右对齐,等宽字体,加千分位" |

---

## 6. 假数据字典(Orchisky 业务风格)

- 物料编码:`M-BRG-A2024-02` / `M-MOT-B24V-07` / `M-CAP-C01-11`
- 单据编码:`CGRK-2604-171175` / `XSCK-2604-090033` / `QTCK-2604-031041`
- 供应商:苏州精密机械有限公司 / 无锡华瑞电机股份有限公司 / 常州浩翔机电设备有限公司
- 仓库:成品库 / 原材料库 / 半成品库 / 不良品库
- 仓位:A01-01-03 / B02-05-12 / C03-08-07
- 数量:3位小数(1.000 / 320.500)，金额:¥12,500.00

---

## 7. 最终检查清单

- [ ] 主色是 #1677FF
- [ ] 顶栏深色 #001529 + 激活 Tab 白底蓝字
- [ ] 侧栏白底 + 激活药丸 #E6F4FF
- [ ] 按钮圆角 6px
- [ ] 表头浅灰 #FAFAFA
- [ ] 状态标签实色填充
- [ ] 表单必填星号在标签前
- [ ] 段落标题无竖线无图标
- [ ] 无 Emoji / 渐变按钮 / 卡片阴影
- [ ] 数字/编码等宽字体 + 右对齐

*配套:《原型设计规范 - Orchisky 平台版 V1.0.md》 · 2026-04-19*
